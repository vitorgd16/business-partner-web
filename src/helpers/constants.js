const URL_BACKEND = "https://localhost:8000/";
const URL_API_SEGMENT = "api/v1/";

const REQUEST_ERROR_CODE_DEFAULT = "ENF";
const REQUEST_ERROR_MESSAGE_DEFAULT = "Opa... Parece que há um problema aqui. Estamos verificando para que consiga continuar!";
const REQUEST_TIMEOUT_MS = (30 * 1000);

export default {
    URL_BACKEND,
    URL_API_SEGMENT,

    REQUEST_ERROR_CODE_DEFAULT,
    REQUEST_ERROR_MESSAGE_DEFAULT,
    REQUEST_TIMEOUT_MS
}